# 第14课时：多模态指令微调与实战
## 1. 从 Caption 到 VQA：Visual Instruction Tuning 的演化路径

在多模态大模型的发展过程中，​**视觉指令微调（Visual Instruction Tuning）并非一蹴而就**​，而是经历了从“被动描述”到“主动理解与决策”的逐步演化。本节将系统梳理这一演化路径，帮助读者理解：​**为什么 VQA 是多模态指令微调的关键形态**​，以及它在整个多模态能力体系中的位置。

![](./assets/vqa.png)

---

### 1.1 Image Caption：从“看见”到“描述”

**Image Captioning（图像描述）** 是最早的视觉-语言任务之一，其目标是让模型​**将图像内容转化为自然语言描述**​。

**任务形式：**

```text
Input : Image
Output: A descriptive sentence
```

**示例：**

> Input：一张胸部 CT 图像
> Output：*“A CT scan of the chest showing bilateral lung opacities.”*

**特点与局限：**

* ✅ 学会基础视觉语义对齐（object / region / attribute）
* ✅ 建立“视觉 → 语言”的映射能力
* ❌ ​**缺乏任务约束**​，输出完全由模型自由生成
* ❌ 无法评估“是否理解正确”，更无法支持决策类任务

📌 **关键结论：**
Image Caption 解决的是 ​**“我看到了什么”**​，而不是 ​**“我该做什么判断”**​。

---

### 1.2 Visual Question Answering（VQA）：从“描述”到“回答问题”

为了让模型具备​**目标导向的视觉理解能力**​，研究者提出了 **Visual Question Answering（VQA）** 任务。

**任务形式：**

```text
Input : Image + Question
Output: Answer
```

**示例（医学场景）：**

> Image：脑部 CT
> Question：*Are regions of the brain infarcted?*
> Answer：*Yes*

与 Image Caption 相比，VQA 引入了 ​**显式的语言约束（Question）**​，迫使模型：

* 聚焦于图像的**特定区域或属性**
* 根据问题意图筛选视觉信息
* 输出**可验证、可评估的结果**

#### 1.2.1 VQA 相较 Caption 的核心进步

| 维度           | Image Caption | VQA      |
| ---------------- | --------------- | ---------- |
| 输出形式       | 自由文本      | 受限答案 |
| 是否有任务目标 | ❌            | ✅       |
| 是否可评估     | 难            | 易       |
| 是否支持决策   | ❌            | ✅       |

📌 **关键转变：**
VQA 不再要求模型“说得多”，而是要求它 ​**“答得对”**​。

---

### 1.3 从 VQA 到 Visual Instruction Tuning 的关键一步

虽然 VQA 的形式是“问答”，但从训练机制上看，它已经具备了​**视觉指令微调（Visual Instruction Tuning）所需的全部要素​**​：

#### 1.3.1 显式指令（Instruction）

医学 VQA 中的问题往往并非开放式提问，而是​**带有明确决策目标的指令**​：

* “Is there evidence of hemorrhage?”
* “Does the gallbladder appear distended?”

这些问题本质上等价于：

> **“请根据图像完成一个判断任务。”**

#### 1.3.2 跨模态条件生成

模型的输出必须同时依赖：

* 视觉输入（图像特征）
* 语言约束（问题语义）

这正是多模态指令微调的核心训练目标。

#### 1.3.3 标准化输出空间

尤其在医学 VQA 中，答案通常是：

* Yes / No
* 器官名称
* 病灶类型

这使得模型行为更加​**稳定、可控、可评测**​。

📌 **核心结论：**

> **VQA 是多模态指令微调的“最小可控形态”，
但它已经完整体现了 Instruction Following 的本质。**

---

### 1.4 Visual Instruction Tuning 的统一视角

从更高层次看，Caption、VQA 与 Visual Instruction 并非割裂任务，而是同一演化链条上的不同阶段：

```text
Image Caption
     ↓
Visual Question Answering (VQA)
     ↓
Visual Instruction Following
     ↓
Multimodal Reasoning & Agent
```

* Caption 解决 **对齐问题**
* VQA 解决 **约束与决策问题**
* Visual Instruction Tuning 解决 **复杂任务执行问题**

在本课程中，我们选择 **VQA-RAD** 作为切入点，正是因为它：

* 数据规模可控
* 指令语义明确
* 医学决策价值高
* 非常适合验证多模态微调效果

---

## 2. 多模态指令数据集：构造多模态 CoT 数据与交错图文处理

在多模态指令微调中，​**数据质量的重要性远高于模型规模**​。相比简单地拼接“图像 + 文本”，高质量的多模态指令数据需要显式刻画​**人类在多模态场景下的认知与推理过程**​。本节将围绕两种关键数据形式展开：

* **多模态 Chain-of-Thought（CoT）数据**
* **交错图文（Interleaved Image-Text）数据**

它们分别解决“​**模型如何推理**​”与“​**模型如何逐步感知多模态信息**​”的问题。

### 2.1 经典多模态（图文）数据集
此类数据集主要用于实现“视觉-文本”的特征对齐，是训练大模型看图说话、以文生图以及多模态理解的基础。

| 数据集 | 领域 | 规模 | 特点 | Schema |
| :--- | :--- | :--- | :--- | :--- |
| **LAION-5B** | 通用多模态 | 58.5亿 图文对 | 全球规模最大的开源图文对，包含URL、文本及CLIP相似度分数 | `{"url": str, "caption": str, "similarity": float}` |
| **ScienceQA** | 科学/教育 | 2.1k 样本 | 多模态科学问答，包含图像、课本上下文及详细的思维链（CoT）解析 | `{"image": img, "hint": str, "solution": str, "lecture": str}` |
| **ShareGPT4V** | 高质量描述 | 1.2M 样本 | 使用 GPT-4V 生成的高度精细化描述，解决了多模态模型描述过短、细节缺失的问题 | `{"image": str, "conversations": List[Dict]}` |
| **OCR-VQA** | 文本OCR | 20万 图像 | 专注于图像中的文字理解，包含书籍封面、路牌等场景，适合提升模型OCR能力 | `{"image_id": int, "questions": List[str], "answers": List[str]}` |

---

### 2.2 专业垂直领域多模态数据集
>以下是“多模态”垂直领域数据.

* **[PathVQA (病理影像多模态)](https://huggingface.co/datasets/flaviagiammarino/path-vqa)**:
    这是医疗多模态的典型代表。它不再是简单的文字病例，而是提供了数万张**病理切片扫描图**，并配有专家的 QA 对。模型必须识别图像中的细胞形态才能得出正确结论。
    ```json
    {
      "image": "pathology_slide_102.jpg",
      "question": "图像中的细胞核是否表现出明显的异型性？",
      "answer": "Yes"
    }
    ```


* **[GeoQA (几何逻辑多模态)](https://huggingface.co/datasets/hz2475/geoQA)**:
    专门针对数学几何题。模型必须解析**几何图形**（如三角形内切圆、辅助线等）与题目文字的关系，进行跨模态的数理推导。
    ```json
    {
      "image": "geometry_05.png",
      "subject": "已知圆 O 的半径为 5，求阴影部分的面积。",
      "comment": ["识别圆形与切线", "应用勾股定理", "计算扇形减去三角形"],
      "answer": "25π/4 

* **[DocVQA（文档理解多模态）](https://huggingface.co/datasets/lmms-lab/DocVQA)**  
  面向**文档级视觉理解**的经典多模态数据集，包含真实扫描的发票、合同、表单等文档图像。
  模型需要同时具备 **OCR 能力、版面结构理解（Layout）以及语义问答能力**。

  ```json
  {
    "image": "invoice_00023.png",
    "question": "What is the total amount due on this invoice?",
    "answer": "1,245.00 USD"
  }
  ```

* **[ChartQA（图表推理多模态）](https://huggingface.co/datasets/ahmed-masry/chartqa)**  
  专注于**统计图表理解与数值推理**的数据集，覆盖柱状图、折线图、饼图等常见图表类型。
  模型需要从图像中读取数值信息，并进行**比较、排序或计算推理**。

  ```json
  {
    "image": "sales_chart_12.png",
    "query": "Which year shows the largest increase in revenue compared to the previous year?",
    "label": "2019"
  }
  ```

---



### 2.3 为什么需要多模态 Chain-of-Thought（CoT）

在标准的 VQA 任务中，训练样本通常采用如下映射关系：

```text
Image + Question → Answer
```

这种监督方式虽然可以让模型在表面上学会“答对问题”，但存在明显局限：

* 模型可能依赖语言先验而非视觉证据（visual shortcut）
* 推理过程完全隐式，难以判断模型是否真正“看懂了图像”
* 在医学、工业等高风险场景中，容易产生**自信但错误的回答**



#### 2.3.1 多模态 CoT 的核心思想

**多模态 Chain-of-Thought（CoT）** 的目标是将人类的多模态推理路径显式注入训练数据中，即：

```text
视觉观察 → 语义解释 → 决策结论
```

通过在监督信号中加入中间推理步骤，模型不仅学习“输出答案”，还学习：

* 应该关注哪些视觉特征
* 如何将视觉证据映射到领域知识
* 如何基于证据完成判断

📌 **关键点：**
多模态 CoT 并不是为了让模型输出“更长的文本”，而是为了​**约束其推理路径必须依赖真实的视觉信息**​。

![](./assets/cot.png)

---

### 2.4 多模态 CoT 的设计原则

在实际构造多模态 CoT 数据时，需要遵循以下原则。

#### 2.4.1 推理必须可追溯到图像证据

❌ 错误示例（与图像弱相关）：

> “根据医学常识，脑梗塞通常会出现低密度影。”

✅ 正确示例（显式依赖视觉信息）：

> “在 CT 图像中，右侧基底节区可见低密度灶，该影像特征通常对应缺血性脑梗塞。”

📌 **原则：**
每一步推理都应能回答一个问题——**“这一判断从图像中的哪里来？”**

---

#### 2.4.2 CoT 是推理链，而不是完整诊断报告

多模态 CoT 的目标并非生成详尽的医学描述，而是突出​**完成当前任务所必需的推理步骤**​。在大多数 VQA 场景中，2–4 步推理已经足够：

1. 关键视觉现象
2. 领域语义解释
3. 最终判断结论

过长的推理文本反而可能引入噪声，降低训练稳定性。

---

#### 2.4.3 最终答案必须结构化、可提取

在训练数据中，应将 **推理过程（Reasoning）** 与 **最终答案（Answer）** 明确区分，例如：

```text
Reasoning: ...
Answer: Yes
```

这样做可以确保：

* 推理内容作为辅助监督信号
* 最终答案作为主要优化目标
* 在评测阶段可直接抽取模型预测结果

---

### 2.5 交错图文（Interleaved Image-Text）的动机与形式

标准 VQA 通常假设：​**单张图像即可回答问题**​。然而在真实场景中，这一假设往往并不成立，例如：

* 多切片 CT / MRI
* 不同视角的 X-ray
* 同一器官的多帧影像

人类医生在阅片时，往往是​**反复切换图像与文字信息，逐步形成判断**​。
交错图文（Interleaved Image-Text）正是对这一过程的建模。

#### 2.5.1 核心思想

> **在一条指令中，多次引入图像，并通过文本对每张图像进行引导与约束。**

其基本结构为：

```text
<image>
文本描述 / 观察结果
<image>
进一步观察
指令 / 问题
```

---

#### 2.5.2 医学示例

```text
<image>
Axial CT slice shows hypodensity in the right basal ganglia.
<image>
Coronal view confirms the extent of the lesion.
Question: Is the infarction acute?
```

📌 **关键理解：**
Interleaved Image-Text 并不是简单地“放多张图”，而是在​**模拟人类逐步查看影像、不断更新认知的过程**​。

---

#### 2.5.3 图文交错代码VQA提取

>这段代码将展示如何将一个简短的图文交错的片段转化为可用的训练数据

```json
import re

def describe_image(url):
    # 模拟一个图像描述函数（真实场景中可由 VLM 生成）
    return "The chest X-ray shows signs of possible pneumonia."

# 一个图文交错的段落（模拟网页 / 文档中的原始文本）
paragraph = """
A chest X-ray is commonly used to examine the lungs and heart.
See the image here: https://fake-med-data.org/images/chest_xray_001.png
In this image, the patient shows signs of possible pneumonia.
"""

# 正则：匹配以 http 开头、以 .png 结尾的 URL
pattern = r"https?://\S+\.png"

# 提取所有匹配的图像链接
image_urls = re.findall(pattern, paragraph)

# 构造 VQA / 指令微调所需字段
text = paragraph.strip()
answer = "The X-ray shows signs consistent with pneumonia."
describe = [describe_image(url) for url in image_urls]

print({
    "text": text,
    "answer": answer,
    "describe": describe
})

```

>输出结果，可以当作训练集中的一条, 结果中的 describe 对应了图片中的内容。

```python

{'text': 'A chest X-ray is commonly used to examine the lungs and heart.\nSee the image here: https://fake-med-data.org/images/chest_xray_001.png\nIn this image, the patient shows signs of possible pneumonia.', 
'answer': 'The X-ray shows signs consistent with pneumonia.', 
'describe': ['The chest X-ray shows signs of possible pneumonia.']}

```

---

### 2.6 数据格式设计与工程实现

在工程实践中，我们将上述思想映射到统一的数据格式中，以下给出三种常见形式。

---

#### 2.6.1 标准 VQA 数据（无 CoT）

```json
{
  "messages": [
    {
      "role": "user",
      "content": "<image> Are regions of the brain infarcted?"
    },
    {
      "role": "assistant",
      "content": "Yes"
    }
  ],
  "images": ["ct_001.jpg"]
}
```

该形式适合基础多模态对齐训练，但对推理能力约束较弱。

---

#### 2.6.2 带多模态 CoT 的指令数据

```json
{
  "messages": [
    {
      "role": "user",
      "content": "<image> Are regions of the brain infarcted?"
    },
    {
      "role": "assistant",
      "content":
        "Reasoning: The CT image shows a hypodense region in the basal ganglia, \
which is a typical imaging feature of ischemic infarction.\n\
Answer: Yes"
    }
  ],
  "images": ["ct_001.jpg"]
}
```

该形式通过显式推理过程，引导模型学习“基于视觉证据作出判断”。

---

#### 2.6.3 交错图文（Interleaved Image-Text）样本

```json
{
  "messages": [
    {
      "role": "user",
      "content":
        "<image> Axial CT slice.\n\
<image> Coronal CT slice.\n\
Question: Does the gallbladder appear distended?"
    },
    {
      "role": "assistant",
      "content": "Answer: Yes"
    }
  ],
  "images": ["ct_axial.jpg", "ct_coronal.jpg"]
}
```

该形式适用于多视角、多阶段视觉理解任务，为后续复杂多模态推理奠定基础。

## 3. 用 LazyLLM 玩转医学多模态推理增强（Img + PPL）

在前面的实验中，我们已经看到，多模态模型可以直接回答医学影像问题。但在真实医学场景中，一个关键挑战是：

👉 小模型在专业医学任务上**无法稳定做出正确预测**  
👉 大模型虽然效果更好，但成本较高  

那么有没有一种方式：

💡 在控制成本的同时，让模型在医学影像任务上表现更可靠？

答案就是：

👉 利用大模型生成高质量数据，提升小模型能力

---

当医生问：

“脑部是否存在梗塞？”

* 普通模型：  
  "yes / no" ❌（容易出错，稳定性不足）

* **增强后的模型：**  
  能够更稳定地给出正确判断 ✅

---

💥 本实验核心目标：

使用 Qwen2.5-VL-32B 生成高质量训练数据 →  
微调 Qwen2.5-VL-3B → 提升医学影像任务预测能力

---

### 3.1 数据集简介

我们使用医学影像 VQA 数据集：

👉 [VQA-RAD](https://huggingface.co/datasets/flaviagiammarino/vqa-rad)（放射影像问答）

![](./assets/exp_dataset.png)

---

#### 3.1.1 数据结构

![](./assets/ds_example.png)

```json
{
  "image": <PIL.Image> (上面图片， 下载时需要自己保存),
  "question": "are regions of the brain infarcted?",
  "answer": "yes"
}
```

---

#### 3.1.2 数据规模

|      | 数量 |
|------|------|
| Train | 1793 |
| Test  | 451  |

---

#### 3.1.3 实验关键点

本实验不直接使用原始 QA，而是：

👉 利用大模型生成更高质量的训练样本

---

### 3.2 数据处理 Pipeline（核心）

---

#### 3.2.0 Step 0：数据下载与转换

```python
from datasets import load_dataset
dataset = load_dataset("flaviagiammarino/vqa-rad")
```

统一转换为：

```json
{
  "image": "path/to/image.jpg",
  "context": "question",
  "reference": "answer"
}
```

---

#### 3.2.1 Step 1：图片预处理

👉 算子：resize_image_inplace  

作用：

* 统一图片尺寸  
* 降低显存占用  
* 提升训练稳定性  

本次实验中统一图片大小为 **336 x 336**

---

#### 3.2.2 Step 2：生成增强数据（关键）

👉 算子：ImageToVQA  根据图片和问题生成完整的推理答案

示例：

```json
{
  "query": "Is the mass hyperintense or hypointense?",
  "answer": "Reasoning: ... Therefore, hyperintense."
}
```

👉 作用：

* 提升数据表达能力  
* 增强模型学习信号  

---

#### 3.2.3 Step 3：数据质量控制

👉 算子：

* PdfQAScorer  给图片和问题打分，主要控制有问题的输出结果。
* qa_score_filter  筛选掉有问题的内容。

📉 数据变化：

| 阶段 | 样本数 |
|------|--------|
| 原始 train | 1793 |
| 过滤后 | 1783 |

---

#### 3.2.4 Step 4：构造训练格式

👉 算子：vqa_to_chat_format  基于Lazyllm的VLM训练格式，把QA对转化为可训练的格式。

```json
{
  "messages": [
    {
      "role": "user",
      "content": "<image>{query}"
    },
    {
      "role": "assistant",
      "content": "{answer}"
    }
  ],
  "images": "path/to/image"
}
```

---

### 3.3 模型微调（SFT）

```python
lazyllm.TrainableModule(model_path, type='vlm') \
    .mode("finetune") \
    .trainset(train_set_path) \
    .finetune_method(
        (
            finetune.llamafactory,
            {
                "learning_rate": 1e-5,
                "cutoff_len": 1024,
                "max_samples": 10000,
                "val_size": 0.1,
                "per_device_train_batch_size": 12,
                "num_train_epochs": 4.0,
                "gradient_accumulation_steps": 10,
            }
        )
    )
```

---

#### 3.3.1 训练策略分析

本实验属于小规模数据（1783条）微调场景，因此需要特别关注训练稳定性。

---

💡 小学习率（1e-5）

* 避免破坏预训练能力  
* 保证参数平稳更新  

---

💡 多轮训练（4 epochs）

* 提升模型对任务的适应能力  

---

💡 梯度累积（10）

👉 等效 batch size：

12 × 10 = 120  

作用：

* 提高梯度稳定性  
* 减少小数据带来的波动  

---

👉 总结：

小学习率 + 大等效 batch + 多轮训练  
是小数据场景下较为稳定的训练策略

---

### 3.4 实验结果

我们使用大预言模型对模型的预测进行打分：

* 如果模型预测和标准答案语义相同得1
* 反之为0

```python

=== infer (score_infer_results.json) ===
  测试总数:  451
  1 的数量: 180  (39.91%)
  0 的数量: 271  (60.09%)
  最终得分: (39.91%)

=== sft (score_sft_results.json) ===
  测试总数:  451
  1 的数量: 209  (46.34%)
  0 的数量: 242  (53.66%)
  最终得分: (46.34%)

```

---

#### 3.4.1 定量结果

| 方法 | 总数 | 正确 | 错误 | 准确率 |
|------|------|------|------|--------|
| Infer（微调前） | 451 | 180 | 271 | 39.91% |
| SFT（微调后）  | 451 | 209 | 242 | 46.34% |

---

#### 3.4.2 提升分析

准确率提升：

39.91% → 46.34%（+6.43%）

样本变化：

* 正确：+29  
* 错误：-29  

---

👉 说明：

SFT 确实提升了模型在医学影像任务上的预测能力

---

### 3.5 结果对比示例（占位）

> 微调前基模型判断图片中有 ‘肾’，预测失败！

```json

  {
    "instruction": "where are the kidney?",
    "image_paths": [
      "/vqa_rad_simple/images/test_4.jpg"
    ],
    "output": "not seen here",
    "prediction": "In this CT scan of the abdomen, the kidneys are located in the lower back region, on either side of the vertebral column. The right kidney is typically positioned higher than the left due to the liver between them.",
    "score": 0
  }

```

> 微调后模型型判断图片中没有 ‘肾’，预测正确！

```json

  {
    "instruction": "where are the kidney?",
    "image_paths": [
      "/vqa_rad_simple/images/test_4.jpg"
    ],
    "output": "not seen here",
    "prediction": "The kidneys are not visible in this CT scan image provided.",
    "score": 1
  }

```

---

### 3.6 总结

本实验核心思路：

👉 利用高质量数据提升模型能力

流程：

1. 构建数据  
2. 数据增强  
3. 数据过滤  
4. 模型微调  

---

👉 最终效果：

模型在医学影像任务上的预测能力得到明显提升

---

