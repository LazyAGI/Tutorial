# 第16课时：Agent 能力增强指南 (Tools & Planning)
欢迎来到 LazyLLM Agent 开发的高阶课堂！ 🚀

本指南基于 [LazyLLM Tutorial 18](https://lazyllm2.readthedocs.io/zh-cn/latest/Tutorial/18/)进行了扩展。我们将从单纯的“概念理解”深入到“代码实现”，涵盖自动工具注册、微调数据格式规范以及训练与推理脚本。

在 LazyLLM 框架中，构建一个强大的 Agent 不仅仅是写提示词，更重要的是准备高质量的数据。本教程将教你如何利用 LazyLLM 的特性，从 Python 代码自动生成工具定义，并构建包含“规划”与“反馈”的微调数据集。

## 🎯 核心目标
1. 工具调用：学习使用 @fc_register 装饰器将 Python 函数转化为 Agent 可用的工具。

2. 数据构建 (Data Construction)：理解并构建包含 tool_calls（工具调用）和 role: tool（环境反馈）的标准对话数据。

3. 规划能力 (Planning)：在数据中注入 ReAct（思考-行动）轨迹，让模型学会拆解问题。

第一部分：让模型拥有“手脚”
大模型无法实时感知天气。我们通过定义 Python 函数并注册到 tool 组中，为模型提供能力。

### 1.1 编写符合规范的 Python 工具函数
```
Python

from typing import Literal
import json
import lazyllm
from lazyllm.tools import fc_register, FunctionCall, FunctionCallAgent

# 使用 @fc_register 注册工具，使其能被 LazyLLM 识别
@fc_register("tool")
def get_current_weather(location: str, unit: Literal["fahrenheit", "celsius"] = "fahrenheit"):
    """
    获取指定地点的实时天气。
    
    Args:
        location (str): 城市名，如 "Tokyo", "Paris"。
        unit (str): 温度单位。
    """
    # 模拟数据
    return json.dumps({"location": location, "temperature": "25", "unit": unit})

@fc_register("tool")
def get_n_day_weather_forecast(location: str, num_days: int, unit: Literal["celsius", "fahrenheit"] = 'fahrenheit'):
    """获取指定地点未来 N 天的天气预报。"""
    return json.dumps({"location": location, "forecast": "Sunny for 3 days"})
```

### 1.2 实现功能调用 (FunctionCall)
FunctionCall 模块的作用是：让模型决定该用哪个工具，并提取出正确的参数。

```
Python

llm = lazyllm.TrainableModule("internlm2-chat-20b").start()

tools = ["get_current_weather", "get_n_day_weather_forecast"]

# 创建功能调用模块
fc = FunctionCall(llm, tools)

query = "What's the weather like today in celsius in Tokyo and Paris."
ret = fc(query)

# 注意：此处 ret 返回的是工具调用的指令/结果，通常是结构化数据
print(f"FC 返回结果: {ret}")
```

## 第二部分：构建微调数据集 (Training Data Structure)
为了让模型学会调用上述工具，我们需要构建符合 ChatML / OpenAI Function Calling 格式的 JSONL 数据。

### 2.1 数据结构概览
一条标准的 Agent 训练数据包含三个关键要素：

* System Prompt: 注入 tools 定义。

* Model Prediction: 模型输出 tool_calls 而不是普通文本。

* Environment Feedback: 添加 role: tool 的消息，告知模型工具执行的结果。

### 2.2 完整数据样例 (JSONL)
```
JSON

{
  "messages": [
    {
      "role": "system",
      "content": "你是一个智能助手，可以调用工具回答问题。"
    },
    {
      "role": "user",
      "content": "北京今天多少度？"
    },
    {
      "role": "assistant",
      "content": null,
      "tool_calls": [
        {
          "id": "call_abc123",
          "type": "function",
          "function": {
            "name": "get_weather",
            "arguments": "{\"city\": \"Beijing\"}"
          }
        }
      ]
    },
    {
      "role": "tool",
      "tool_call_id": "call_abc123",
      "name": "get_weather",
      "content": "{\"city\": \"Beijing\", \"temp\": 25, \"unit\": \"celsius\"}"
    },
    {
      "role": "assistant",
      "content": "北京今天气温为 25 摄氏度。"
    }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "获取指定城市的当前天气情况。",
        "parameters": { ... } 
      }
    }
  ]
}
```

## 第三部分：给模型“装脑” —— 规划与状态保持
面对复杂任务（如：“先查天气，如果是雨天就帮我取消航班”），简单的工具调用是不够的。我们需要在数据中显式地教模型进行 ReAct (Reasoning + Acting)。

### 3.1 规划能力数据集 (Planning Data)
在 assistant 的回复中，除了 tool_calls，还可以包含 content 字段用于输出思考过程（Thought）。

数据构建示例（ReAct 模式）：
![ReAct](./img1.png)
```
JSON

{
  "role": "user",
  "content": "帮我查下上海的天气，适合去迪士尼吗？"
},
{
  "role": "assistant",
  "content": "Thought: 用户想去迪士尼，我需要先查询上海的天气，然后根据天气情况给出建议。\nAction: 调用 get_weather 工具。",
  "tool_calls": [
    {
      "function": { "name": "get_weather", "arguments": "{\"city\": \"Shanghai\"}" }
    }
  ]
}
```

### 3.2 模拟环境反馈 (Environment Feedback & Error Handling)
为了增强 Agent 的鲁棒性，训练数据中必须包含工具调用失败的场景。这教会模型在遇到错误时自我修正，而不是崩溃。

错误恢复数据样例：

* Model: 调用 get_weather(city="ShangHai") (拼写大小写不标准)

* Tool (Environment): Error: City not found. Please provide standard city name.

* Model (Correction): Thought: 工具提示找不到城市，我应该尝试使用标准的中文拼音或汉字。\nCall: get_weather(city="Shanghai")

## 🚀 总结：LazyLLM Agent 开发流程
1. 定义 (Define)：在 tools.py 中编写 Python 函数，严格遵守 Args: 文档格式。

2. 生成 (Generate)：运行脚本调用 tm.tools_desc 导出 JSON Schema。

3. 合成 (Synthesize)：使用 GPT-4 或 DeepSeek 构造包含 ReAct 思考链和工具调用轨迹的对话数据（JSONL）。

4. 训练 (Train)：使用 LazyLLM 的微调模块（开启 mode='fc'）进行训练。