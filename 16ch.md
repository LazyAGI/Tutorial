# 第16课时：Agent 能力增强指南 (Tools & Planning)
欢迎来到 LazyLLM Agent 开发的高阶课堂！ 🚀

本指南基于 [LazyLLM Tutorial 18](https://lazyllm2.readthedocs.io/zh-cn/latest/Tutorial/18/)进行了扩展。我们将从单纯的“概念理解”深入到“代码实现”，涵盖自动工具注册、微调数据格式规范以及训练与推理脚本。

在 LazyLLM 框架中，构建一个强大的 Agent 不仅仅是写提示词，更重要的是准备高质量的数据。本教程将教你如何利用 LazyLLM 的特性，从 Python 代码自动生成工具定义，并构建包含“规划”与“反馈”的微调数据集。

## 📚 目录
1. 基础篇：API 定义与工具注册 —— 给模型一张“说明书”

2. 进阶篇：规划能力 (Planning) 数据合成 —— 教模型“三思而后行”

3. 高阶篇：环境反馈与状态保持 —— 模拟真实世界的挫折与连续性

4. 实战篇：LazyLLM 微调数据格式详解 —— 手把手构建 JSONL

5. 避坑指南：常见错误与调试技巧

## 🛠 第一章：基础篇 —— API 定义与工具注册 (Tool Use)
模型本身不能联网、不会算数。我们需要通过定义 API 接口来扩展它的能力。在 LazyLLM 中，我们通过“代码即定义”的方式来实现。

### 1.1 Pythonic 工具定义 (Code-First)
LazyLLM 支持通过 Python 函数直接生成工具 Schema。

核心原则：

Type Hinting：必须使用 Python 标准类型提示 (str, int, Literal)。

Google-Style Docstring：这是模型理解工具用途的唯一途径，必须包含 Args 描述。
```
Python

from typing import Literal, Dict
import json
from lazyllm.tools import fc_register

# 使用装饰器注册工具，'tool' 是组名
@fc_register("tool")
def book_flight(
    origin: str, 
    destination: str, 
    seat_class: Literal["economy", "business", "first"] = "economy"
):
    """
    预订从出发地到目的地的航班机票。

    Args:
        origin (str): 出发城市的三字代码，如 PEK, SHA。
        destination (str): 目的城市的三字代码。
        seat_class (str): 舱位等级，默认为经济舱。
    """
    # 模拟业务逻辑
    return json.dumps({
        "status": "success", 
        "ticket_id": "T12345", 
        "price": 1200
    })
```

### 1.2 自动生成调用 Schema
模型并不直接运行 Python 代码，它阅读的是 JSON Schema。
```
Python

from lazyllm.tools import ToolManager

# 初始化工具管理器
tm = ToolManager([book_flight])

# 导出 OpenAI 兼容的 Schema
# 提示：可以直接将此 Schema 放入 system prompt 中进行测试
print(tm.tools_desc)
```

## 🧠 第二章：进阶篇 —— 规划能力数据合成 (Planning Data)
面对复杂问题（例如：“先查天气，如果是晴天就帮我订票，如果是雨天就查附近的博物馆”），模型不能直接瞎猜，需要规划。我们需要合成以下两种模式的数据：

### 2.1 ReAct 模式 (Reasoning + Acting)

React 主要包括以下的流程：

1. 思考（Thought）: Agent 在收到 query 后，它会先给出下一步要采取的行动；
2. 行动（Action）: Agent 会采取并执行一个行动，比如使用工具（或者继续思考）；
3. 观察（Observation）: Agent 观察行动的反馈，比如工具的输出；
上面过程也是会不断循环往复，直到满足 query 的请求，或者达到了最大的迭代次数。
![ReAct](./img1.png)
ReactAgent 执行流程和 FunctionCallAgent 的执行流程一样，唯一区别是 prompt 不同，并且 ReactAgent 每一步都要有 Thought 输出，而普通 FunctionCallAgent 可能只有工具调用的信息输出，没有 content 内容。示例如下：

```
Python
import lazyllm
from lazyllm.tools import fc_register, ReactAgent
@fc_register("tool")
def multiply_tool(a: int, b: int) -> int:
    return a * b
@fc_register("tool")
def add_tool(a: int, b: int):
    return a + b
tools = ["multiply_tool", "add_tool"]
llm = lazyllm.OnlineChatModule(source="sensenova", model="DeepSeek-V3")
agent = ReactAgent(llm, tools)
query = "What is 20+(2*4)? Calculate step by step."
res = agent(query)
print(res)
```

数据合成模板：

    User: "我想买一台大概 5000 元的笔记本电脑，推荐一款。" Assistant (Thought): 用户没有说明品牌偏好，我应该先搜索当前市场上 5000 元价位段的热门笔记本型号。 Assistant (Call): search_products(category="laptop", price_range="4000-6000")

### 2.2 Plan-and-Solve 模式 (Task Decomposition)

PlanAndSolve 主要包括以下的流程：

计划（Plan）：Agent 在收到 query 后，它会将这个任务分解为更小的子任务；
行动（Action）: Agent 对当前的子任务进行执行；
观察（Observation）: Agent 观察当前行动的结果，如果解决问题就返回，如果仅解决当前子任务就继续执行计划，如果没解决当前子任务就重新计划后续步骤；
![ReAct](./img2.png)
* 注意： 上图中 ② Action x1 表示每次行动只执行一个子任务（不会全部将子任务执行完，区别 ReWOO的对应流程中的 ② Action xN）。
PlanAndSolveAgent由两个组件组成：首先，将整个任务分解为更小的子任务，其次，根据计划执行这些子任务。最后结果作为答案进行输出。

```
Python
import lazyllm
from lazyllm.tools import fc_register, PlanAndSolveAgent
@fc_register("tool")
def multiply(a: int, b: int) -> int:
    return a * b
@fc_register("tool")
def add(a: int, b: int):
    return a + b
llm = lazyllm.OnlineChatModule(source="sensenova", model="DeepSeek-V3")
tools = ["multiply", "add"]
agent = PlanAndSolveAgent(llm, tools=tools)
query = "What is 20+(2*4)? Calculate step by step."
ret = agent(query)
print(ret)
```

数据合成模板：

    User: "帮我分析一下这两家公司的财报差异。" Assistant (Plan):

    1. 调用 get_financial_report 获取 A 公司财报。

    2. 调用 get_financial_report 获取 B 公司财报。

    3. 对比两者的净利润和营收增长率。

    4. 生成总结报告。



## 第三章：高阶篇 —— 环境反馈与状态保持
真实世界是不完美的。工具会报错，网络会超时。高质量的 Agent 数据必须包含 负样本 (Negative Samples) 和 多轮状态 (State Keeping)。

### 3.1 模拟环境报错 (Error Handling)
教会模型：当工具报错时，不要复读，要尝试修正参数。

    User: "查下 Shanhai 的天气。" (故意拼错)

    Model: get_weather(city="Shanhai")

    Tool (Observation): Error: City 'Shanhai' not found. Did you mean 'Shanghai'?

    Model (Correction/Thought): "我拼写错了城市名，工具提示应该是 Shanghai。我将重新调用。"

    Model (Retry): get_weather(city="Shanghai")

### 3.2 多轮对话中的状态保持
模型需要记住之前的操作结果。

    Turn 1:

        User: "帮我查去北京的票。"

        Agent: (调用查票工具) -> "查到了，G101 次，10:00 出发。"

    Turn 2:

        User: "帮我订这张。" (注意：用户没说订哪张，Agent 需利用上下文)

        Agent (Thought): "根据上下文，用户指的是刚才查到的 G101 次列车。"

        Agent (Call): book_ticket(train_id="G101")

## 💻 第四章：实战篇 —— LazyLLM 微调数据格式详解
在 LazyLLM 中，我们通常使用 JSONL 格式来组织这些数据。以下是一个包含 System Prompt注入、工具调用、环境反馈 的完整微调样本。

### 4.1 标准数据结构 (fc_train.jsonl)
```
JSON

{
  "messages": [
    {
      "role": "system",
      "content": "你是一个智能助手。请使用工具解决用户问题。"
    },
    {
      "role": "user",
      "content": "北京今天天气怎么样？"
    },
    {
      "role": "assistant",
      "content": null, 
      "tool_calls": [
        {
          "id": "call_123456",
          "type": "function",
          "function": {
            "name": "get_current_weather",
            "arguments": "{\"location\": \"Beijing\", \"unit\": \"celsius\"}"
          }
        }
      ]
    },
    {
      "role": "tool",
      "tool_call_id": "call_123456",
      "name": "get_current_weather",
      "content": "{\"temperature\": 28, \"condition\": \"Sunny\"}"
    },
    {
      "role": "assistant",
      "content": "北京今天天气晴朗，气温 28 摄氏度。"
    }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_current_weather",
        "description": "获取指定地点的实时天气。",
        "parameters": {
            "type": "object",
            "properties": {
                "location": {"type": "string", "description": "城市名"},
                "unit": {"type": "string", "enum": ["celsius", "fahrenheit"]}
            },
            "required": ["location"]
        }
      }
    }
  ]
}
```

## ⚠️ 第五章：避坑指南
根据以往的调试经验，以下是开发 Agent 时的常见雷区：

1. Docstring 格式陷阱：

* ❌ 错误：Args: city: 城市名 (格式混乱)

* ✅ 正确：Args:\n city (str): 城市名 (严格缩进和类型标注)

* 后果：LazyLLM 抛出 TypeError，无法生成 Schema。

2. 序列化陷阱：

* 不要直接打印 tm.all_tools，那是一个对象列表。

* 请使用 tm.tools_desc 或遍历列表调用 .to_dict() 获取可读的 JSON。

3. 幻觉调用 (Hallucination)：

* 如果训练数据过少，模型可能会捏造不存在的工具（如 call_super_ai()）。

* 解决：在 System Prompt 中强制约束：“只能使用提供的工具，如无合适工具请直接回答无法处理。”

## 🚀 总结：LazyLLM Agent 开发流程
1. 定义 (Define)：在 tools.py 中编写 Python 函数，严格遵守 Args: 文档格式。

2. 生成 (Generate)：运行脚本调用 tm.tools_desc 导出 JSON Schema。

3. 合成 (Synthesize)：使用 GPT-4 或 DeepSeek 构造包含 ReAct 思考链和工具调用轨迹的对话数据（JSONL）。

4. 训练 (Train)：使用 LazyLLM 的微调模块（开启 mode='fc'）进行训练。